# Owen's vscode setting

-  Clone this repo
-  Download Sync Setting Extension in vscode
-  config Sync Setting Extension `settings.yml` with mine `settings.yml`
-  use download setting command to download setting